export interface Product {
    name: string;
    description: string;
    price: string;
    img: string;
    color: string;
    id: string;
    myProduct?: Boolean;
}

export interface Author {
    name: string;
    id: string;
}